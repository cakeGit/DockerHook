# Copy this file to .env and edit values as needed.
# The auth token used by clients (GitHub webhook or CI) to call the API.
AUTH_TOKEN=change_me_to_a_strong_token
# Dev test mode (for local development, ignore actually requiring the docker compose file)
DEV_TEST_MODE=false
# Port to listen on
PORT=3000
# Path to the docker-compose file on the Debian server (read-only)
COMPOSE_PATH=/root/docker_compose.yml
# Directory where webhook triggers are written. The server-side updater should watch this.
TRIGGER_DIR=/var/run/dockerhook
# Maximum number of requests per minute per IP
RATE_LIMIT_PER_MINUTE=30
